<div class="container">
  <div class="container mt-4">
    <h1 class="mb-3"><%= "#{@anime.title} (#{@figures.count})" %> </h1>
    <div class="content-container">
      <div class="figures-container">
        <%= render 'shared/figure_card' %>
      </div>
    </div>
  </div>
</div>
